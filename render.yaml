services:
  - type: web
    name: vitamind-predictor
    runtime: python
    rootDir: deploy_app
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
